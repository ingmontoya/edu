*.log
.DS_Store
/k8s/secrets.yaml
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Kubernetes secrets — never commit with real values
# The placeholder version (with CHANGE_ME) can be committed as a template,
# but if you fill in real values, add to a separate file not tracked by git.
# k8s/secrets.yaml
